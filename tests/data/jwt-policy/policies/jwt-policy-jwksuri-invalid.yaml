apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: jwt-policy-invalid
spec:
  jwt:
    realm: MyProductAPI
    token: $http_token
    secret: jwk-secret-valid
    jwksURI: https://myidp.exmaple.com/certs
    keyCache: 1h
